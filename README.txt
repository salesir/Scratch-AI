This will be an easy to read update log.
I have excluded the large data files, they can be found in huggingface, this AI was tested on wikipedia and gutenberg book collections
remember that changing hyperparameters would result in a new checkpoint, checkpoints can be freely switched around but the model must be switched around aswell
next major update will include a fix so that this code can work with google colab so it can be trained online
